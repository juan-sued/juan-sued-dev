"use client";
import { useState } from "react";
import type { Locale } from "@/content/data";
import { track } from "@/lib/analytics";
import { createClient } from "@/lib/supabase/client";
import { toast } from "sonner";
export function Lab({ locale }: { locale: Locale }) { const [state, setState] = useState(0); const states = locale === "pt" ? [["Pendente", "Captura guardada localmente, aguardando conexão."], ["Enviando", "Fila sendo sincronizada com serviço remoto."], ["Tentando novamente", "Falha temporária; nova tentativa agendada."], ["Confirmado", "Processamento confirmado e fila atualizada."]] : [["Pending", "Capture stored locally, waiting for connection."], ["Sending", "Queue synchronizing with remote service."], ["Retrying", "Temporary failure; another attempt is scheduled."], ["Confirmed", "Processing confirmed and queue updated."]]; return <div className="card p-6" aria-label={locale === "pt" ? "Simulação de fila offline" : "Offline queue simulation"}><p className="font-semibold">{locale === "pt" ? "Simulação de fila offline" : "Offline queue simulation"}</p><div className="mt-6 grid gap-3 sm:grid-cols-4">{states.map(([name], index) => <button key={name} onClick={() => setState(index)} aria-pressed={state === index} className={`rounded-lg border p-4 text-left ${state === index ? "border-[var(--brand)] bg-[var(--brand-soft)]" : "border-[var(--line)]"}`}><span aria-hidden="true" className="text-xs text-[var(--muted)]">0{index + 1}</span><strong className="mt-2 block">{name}</strong></button>)}</div><p aria-live="polite" className="mt-5 text-sm text-[var(--muted)]"><strong>{states[state][0]}: </strong>{states[state][1]}</p></div>; }
export function ContactForm({ locale }: { locale: Locale }) { const [feedback, setFeedback] = useState(""); const [loading, setLoading] = useState(false); const label = locale === "pt" ? { name: "Nome", message: "Mensagem", action: "Enviar mensagem", feedback: "Mensagem enviada. Obrigado pelo contato!", invalid: "Revise os campos obrigatórios.", error: "Não foi possível enviar. Tente novamente." } : { name: "Name", message: "Message", action: "Send message", feedback: "Message sent. Thank you for reaching out!", invalid: "Review required fields.", error: "Unable to send. Please try again." }; return <form onSubmit={async event => { event.preventDefault(); const form = new FormData(event.currentTarget); const name = String(form.get("name") ?? "").trim(); const email = String(form.get("email") ?? "").trim(); const message = String(form.get("message") ?? "").trim(); if (name.length < 2 || message.length < 10 || !/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email)) { setFeedback(label.invalid); return; } setLoading(true); const { error } = await createClient().from("contact_submissions").insert({ name, email, message }); setLoading(false); if (error) { setFeedback(label.error); toast.error(label.error); return; } event.currentTarget.reset(); track("email_click", { source: "form" }); setFeedback(label.feedback); toast.success(label.feedback); }} className="card grid gap-3 p-5"><label htmlFor="contact-name" className="text-sm font-bold">{label.name}</label><input id="contact-name" name="name" required minLength={2} maxLength={80} autoComplete="name" className="-mt-2 rounded-lg border border-[var(--line)] bg-transparent p-3"/><label htmlFor="contact-email" className="text-sm font-bold">E-mail</label><input id="contact-email" name="email" required type="email" maxLength={120} autoComplete="email" className="-mt-2 rounded-lg border border-[var(--line)] bg-transparent p-3"/><label htmlFor="contact-message" className="text-sm font-bold">{label.message}</label><textarea id="contact-message" name="message" required minLength={10} maxLength={2000} rows={4} className="-mt-2 rounded-lg border border-[var(--line)] bg-transparent p-3"/><button disabled={loading} className="rounded-lg bg-[var(--brand)] p-3 font-bold text-white">{loading ? "..." : label.action}</button><p aria-live="polite" role="status" className="min-h-5 text-sm text-[var(--brand)]">{feedback}</p></form>; }
