import { saveOpportunity } from "@/app/admin/crm/actions";
const statuses=["prospect","applied","recruiter_contact","screening","technical_interview","final_interview","offer","hired","rejected","withdrawn"];
type Opportunity = Record<string, string | number | null | undefined>;
export function OpportunityForm({ opportunity }: { opportunity?: Opportunity }) { const val=(key:string)=>String(opportunity?.[key] || ""); return <form action={saveOpportunity} className="mt-6 grid max-w-3xl gap-4"><input type="hidden" name="id" value={val("id")}/><Field name="title" label="Título" value={val("title")} required/><Field name="company_name" label="Empresa" value={val("company_name")}/><Field name="contact_id" label="ID do contato (opcional)" value={val("contact_id")}/><div className="grid gap-4 sm:grid-cols-2"><Field name="employment_type" label="Tipo de contratação" value={val("employment_type")}/><Field name="work_model" label="Modelo de trabalho" value={val("work_model")}/></div><div className="grid gap-4 sm:grid-cols-3"><Field name="salary_min" label="Salário mínimo" value={val("salary_min")} type="number"/><Field name="salary_max" label="Salário máximo" value={val("salary_max")} type="number"/><Field name="currency" label="Moeda" value={val("currency") || "BRL"} required/></div><label className="grid gap-1 text-sm font-semibold">Status<select name="status" defaultValue={val("status") || "prospect"} className="rounded-lg border border-[var(--line)] bg-transparent p-3">{statuses.map(x=><option key={x}>{x}</option>)}</select></label><Field name="source" label="Origem" value={val("source")}/><Field name="job_url" label="URL da vaga" value={val("job_url")} type="url"/><div className="grid gap-4 sm:grid-cols-2"><Field name="next_action_at" label="Próxima ação" value={val("next_action_at").slice(0,16)} type="datetime-local"/><Field name="interview_at" label="Entrevista" value={val("interview_at").slice(0,16)} type="datetime-local"/></div><Field name="closed_at" label="Encerramento" value={val("closed_at").slice(0,16)} type="datetime-local"/><label className="grid gap-1 text-sm font-semibold">Notas<textarea name="notes" defaultValue={val("notes")} maxLength={5000} className="min-h-32 rounded-lg border border-[var(--line)] bg-transparent p-3"/></label><button className="w-fit rounded-lg bg-[var(--brand)] px-4 py-3 font-semibold text-white">{opportunity ? "Salvar oportunidade" : "Criar oportunidade"}</button></form>; }
function Field({ name,label,value,type="text",required=false }: { name:string;label:string;value:string;type?:string;required?:boolean }) { return <label className="grid gap-1 text-sm font-semibold">{label}<input name={name} type={type} defaultValue={value} required={required} min={type === "number" ? 0 : undefined} className="rounded-lg border border-[var(--line)] bg-transparent p-3"/></label>; }
