import Image from "next/image";
import Link from "next/link";
import { ArrowDownToLine, ArrowUpRight, ChevronRight, MapPin } from "lucide-react";
import { cases, experiences, profile, skills, text, type Locale } from "@/content/data";
import { ContactForm, Lab } from "@/components/client-widgets";
const pt = (locale: Locale, a: string, b: string) => locale === "pt" ? a : b; const external = "inline-flex items-center gap-1 font-bold";
function Section({ title, children }: { title: string; children: React.ReactNode }) { return <section className="shell py-12 md:py-20"><h2 className="mb-8 text-3xl font-semibold md:text-4xl">{title}</h2>{children}</section>; }
export function Home({ locale }: { locale: Locale }) { return <><section className="grid-bg border-b border-[var(--line)]"><div className="shell grid min-h-[500px] items-center gap-8 py-12 md:grid-cols-[1.35fr_.65fr] md:py-20"><div><p className="eyebrow">{profile.location[locale]}</p><h1 className="mt-4 text-5xl font-semibold md:text-7xl">Juan Sued<span className="block text-[var(--brand)]">{text(profile.role, locale)}</span></h1><p className="mt-6 max-w-2xl text-lg text-[var(--muted)]">{text(profile.intro, locale)}</p><div className="mt-8 flex flex-wrap gap-3"><Link href="/experiencia" className="flex items-center gap-2 rounded-lg bg-[var(--brand)] px-4 py-3 font-bold text-white">{pt(locale, "Ver experiência", "View experience")}<ChevronRight size={17}/></Link><Link href="/projetos" className="rounded-lg border border-[var(--line)] px-4 py-3 font-bold">{pt(locale, "Explorar projetos", "Explore projects")}</Link></div></div><div className="hero-photo relative mx-auto w-full max-w-[220px] md:max-w-sm"><div className="relative aspect-square overflow-hidden rounded-2xl border border-[var(--line)]"><Image src="/images/juan-sued-profile.png" alt="Juan Sued" fill priority className="object-cover" style={{ objectPosition: "52% 25%" }} sizes="(max-width: 767px) 220px, 384px"/></div></div></div></section><Section title={pt(locale, "Resumo em 30 segundos", "30-second summary")}><div className="grid gap-3 sm:grid-cols-2 lg:grid-cols-3">{(locale === "pt" ? ["Experiência profissional em tecnologia desde 2022", "Projetos independentes desde 2018", "Web, Android e iOS", "Múltiplos frontends e microserviços", "React Native, React, NestJS, Redis e PostgreSQL", "Geolocalização, tempo real e fluxos operacionais"] : ["Technology experience since 2022", "Independent projects since 2018", "Web, Android and iOS", "Multiple frontends and microservices", "React Native, React, NestJS, Redis and PostgreSQL", "Geolocation, real-time and operational flows"]).map(item => <div key={item} className="card p-5">{item}</div>)}</div></Section><Section title={pt(locale, "Experiência recente", "Recent experience")}><Experience locale={locale} limit={2}/></Section><Section title={pt(locale, "Projetos", "Projects")}><Projects locale={locale} embedded/></Section><Section title={pt(locale, "Laboratório técnico", "Technical lab")}><Lab locale={locale}/></Section><Resume locale={locale} embedded/><Contact locale={locale} embedded/></>; }
export function Experience({ locale, limit }: { locale: Locale; limit?: number }) { const items = limit ? experiences.slice(0, limit) : experiences; return <ol className="border-l border-[var(--line)]">{items.map(item => <li key={item.company} className="relative pb-10 pl-7"><span aria-hidden="true" className="absolute -left-[5px] top-2 size-2.5 rounded-full bg-[var(--brand)]"/><p className="text-sm font-bold text-[var(--brand)]">{text(item.period, locale)}</p><h2 className="mt-1 text-xl font-semibold">{item.company}</h2><p>{text(item.role, locale)}</p><ul className="mt-4 grid gap-2 text-sm text-[var(--muted)]">{item.points[locale].map(point => <li key={point}>- {point}</li>)}</ul></li>)}</ol>; }
export function ExperiencePage({ locale }: { locale: Locale }) { return <section className="shell py-14 md:py-20"><h1 className="mb-10 text-4xl font-semibold">{pt(locale, "Experiência profissional", "Professional experience")}</h1><Experience locale={locale}/></section>; }
export function Projects({ locale, embedded = false }: { locale: Locale; embedded?: boolean }) { const projects = [{ name: "BikerWay", desc: pt(locale, "Projeto autoral para motociclistas com comboios, eventos, SOS, rotas e pontos de apoio.", "Author project for motorcyclists with rides, events, SOS, routes and support points."), url: "https://bikerway.com.br/", stack: "React, TypeScript, Supabase, PostgreSQL, PostGIS", case: "/projetos/bikerway" }, { name: "Event Horizon", desc: pt(locale, "Marca para projetos independentes e soluções digitais para pequenas empresas.", "Brand for independent projects and digital solutions for small businesses."), url: "https://event-horizon-by-juan-sued.vercel.app/", stack: "Frontend, backend, integrations, deployment" }]; return <div className={embedded ? "grid gap-5 md:grid-cols-2" : "shell grid gap-5 py-14 md:grid-cols-2 md:py-20"}>{projects.map(project => <article key={project.name} className="card p-6"><p className="eyebrow">{project.name}</p><p className="mt-3 text-[var(--muted)]">{project.desc}</p><p className="mt-4 text-sm font-bold text-[var(--brand)]">{project.stack}</p><div className="mt-6 flex gap-3"><a href={project.url} target="_blank" rel="noopener noreferrer" className={external}>{locale === "pt" ? `Conhecer projeto ${project.name}` : `Visit ${project.name} project`}<ArrowUpRight size={15}/></a>{project.case && <Link href={project.case}>{pt(locale, "Estudo de caso", "Case study")}</Link>}</div></article>)}</div>; }
export function BikerWay({ locale }: { locale: Locale }) { return <section className="shell py-14 md:py-20"><h1 className="text-4xl font-semibold">BikerWay</h1><p className="mt-6 max-w-2xl text-lg text-[var(--muted)]">{pt(locale, "Plataforma social e utilitária para motociclistas com comboios, eventos, SOS, rotas e pontos de apoio.", "Social and utility platform for motorcyclists with rides, events, SOS, routes and support points.")}</p><Section title={pt(locale, "Decisão espacial", "Spatial decision")}><div className="grid gap-5 md:grid-cols-2"><article className="card p-6"><h2>H3</h2><p>{pt(locale, "Referência aplicada em análises geográficas profissionais.", "Reference applied in professional geographic analysis.")}</p></article><article className="card p-6"><h2>PostGIS</h2><p>{pt(locale, "Escolhido no BikerWay para consultas de raio e distância.", "Selected in BikerWay for radius and distance queries.")}</p></article></div></Section></section>; }
export function CasePage({ locale, slug }: { locale: Locale; slug: string }) { const item = cases.find(candidate => candidate.slug === slug)!; return <section className="shell py-14 md:py-20"><p className="eyebrow">{item.tag}</p><h1 className="mt-3 text-4xl font-semibold md:text-6xl">{text(item.title, locale)}</h1><p className="mt-5 text-lg text-[var(--muted)]">{text(item.summary, locale)}</p><div className="mt-10 grid gap-4 md:grid-cols-2">{item.sections[locale].map(([heading, body]) => <article key={heading} className="card p-6"><h2 className="text-xl font-semibold">{heading}</h2><p className="mt-3 text-[var(--muted)]">{body}</p></article>)}</div></section>; }
export function About({ locale }: { locale: Locale }) { return <section className="shell py-14 md:py-20"><h1 className="text-4xl font-semibold">{text(profile.role, locale)}</h1><div className="mt-10 grid gap-4 md:grid-cols-2">{skills.map(([name, list]) => <article className="card p-5" key={name}><h2>{name}</h2><p className="mt-3 text-[var(--muted)]">{list}</p></article>)}</div></section>; }
export function Resume({ locale, embedded = false }: { locale: Locale; embedded?: boolean }) { const cards = <div className="grid gap-4 md:grid-cols-2"><a download href="/curriculos/juan-sued-cv-ats.pdf" className="card p-6"><h2>{pt(locale, "Currículo ATS", "ATS resume")}</h2><span className="mt-4 flex gap-2"><ArrowDownToLine size={18}/>{pt(locale, "Baixar PDF", "Download PDF")}</span></a><a download href="/curriculos/juan-sued-cv-visual.pdf" className="card p-6"><h2>{pt(locale, "Currículo visual", "Visual resume")}</h2><span className="mt-4 flex gap-2"><ArrowDownToLine size={18}/>{pt(locale, "Baixar PDF", "Download PDF")}</span></a></div>; return embedded ? <Section title={pt(locale, "Currículos", "Resumes")}>{cards}</Section> : <section className="shell py-14 md:py-20"><h1 className="mb-8 text-4xl font-semibold">{pt(locale, "Currículos", "Resumes")}</h1>{cards}</section>; }
export function Contact({ locale, embedded = false }: { locale: Locale; embedded?: boolean }) { const body = <div className="grid gap-8 md:grid-cols-2"><div className="grid gap-3"><a href="https://www.linkedin.com/in/juan-sued/" target="_blank" rel="noopener noreferrer">LinkedIn</a><a href="https://github.com/juan-sued" target="_blank" rel="noopener noreferrer">GitHub</a><a href="mailto:juansued19@gmail.com">juansued19@gmail.com</a></div><ContactForm locale={locale}/></div>; return embedded ? <Section title={pt(locale, "Contato", "Contact")}>{body}</Section> : <section className="shell py-14 md:py-20"><h1 className="mb-8 text-4xl font-semibold">{pt(locale, "Contato", "Contact")}</h1>{body}</section>; }
export function Footer({ locale }: { locale: Locale }) { return <footer className="border-t border-[var(--line)] py-8"><div className="shell flex justify-between text-sm text-[var(--muted)]"><span>Juan Sued</span><span className="flex gap-1"><MapPin size={15}/>{profile.location[locale]}</span></div></footer>; }
