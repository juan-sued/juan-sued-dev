import { ExperiencePage, Footer } from "@/components/server-content"; import { getPreferences } from "@/lib/preferences"; import { pageMetadata } from "@/lib/seo"; export const metadata = pageMetadata("/experiencia", "Experiência profissional"); export default async function Page() { const { locale } = await getPreferences(); return <main id="content"><ExperiencePage locale={locale}/><Footer locale={locale}/></main>; }
