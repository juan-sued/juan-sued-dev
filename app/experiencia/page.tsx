import { ExperiencePage, Footer } from "@/components/server-content"; import { getPreferences } from "@/lib/preferences"; import { getExperiences } from "@/lib/repositories/experiences"; import { getCachedProfile } from "@/lib/repositories/profile-cache"; import { pageMetadata } from "@/lib/seo"; export const metadata = pageMetadata("/experiencia", "Experiência profissional"); export default async function Page() { const [{ locale }, experiences, profile] = await Promise.all([getPreferences(), getExperiences(), getCachedProfile()]); return <main id="content"><ExperiencePage locale={locale} experiences={experiences}/><Footer locale={locale} profile={profile}/></main>; }
