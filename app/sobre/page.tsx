import { About, Footer } from "@/components/server-content"; import { getPreferences } from "@/lib/preferences"; import { getEducation } from "@/lib/repositories/education"; import { getSkills } from "@/lib/repositories/skills"; import { pageMetadata } from "@/lib/seo"; export const metadata = pageMetadata("/sobre", "Sobre"); export default async function Page() { const [{ locale }, education, skills] = await Promise.all([getPreferences(), getEducation(), getSkills()]); return <main id="content"><About locale={locale} education={education} skills={skills}/><Footer locale={locale}/></main>; }
