import type { MetadataRoute } from "next"; import { siteUrl } from "@/lib/site"; export default function sitemap(): MetadataRoute.Sitemap { return ["", "/experiencia", "/projetos", "/projetos/bikerway", "/cases/h3", "/cases/estado", "/cases/offline", "/cases/localizacao", "/sobre", "/contato", "/curriculo"].map(path => ({ url: `${siteUrl}${path}`, lastModified: new Date(), changeFrequency: "monthly", priority: path === "" ? 1 : .7 })); }
