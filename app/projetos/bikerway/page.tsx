import { BikerWay, Footer } from "@/components/server-content"; import { getPreferences } from "@/lib/preferences"; import { pageMetadata } from "@/lib/seo"; export const metadata = pageMetadata("/projetos/bikerway", "BikerWay e PostGIS - Estudo de caso de Juan Sued", "Como PostgreSQL e PostGIS foram escolhidos para consultas de proximidade, raio e distância em uma plataforma para motociclistas."); export default async function Page() { const { locale } = await getPreferences(); return <main id="content"><BikerWay locale={locale}/><Footer locale={locale}/></main>; }
