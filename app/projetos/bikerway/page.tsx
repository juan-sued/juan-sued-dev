import { BikerWay, Footer } from "@/components/server-content"; import { getPreferences } from "@/lib/preferences"; import { pageMetadata } from "@/lib/seo"; export const metadata = pageMetadata("/projetos/bikerway", "BikerWay"); export default async function Page() { const { locale } = await getPreferences(); return <main id="content"><BikerWay locale={locale}/><Footer locale={locale}/></main>; }
