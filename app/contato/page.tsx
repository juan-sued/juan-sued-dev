import { Contact, Footer } from "@/components/server-content"; import { getPreferences } from "@/lib/preferences"; import { getCachedProfile } from "@/lib/repositories/profile-cache"; import { pageMetadata } from "@/lib/seo"; export const metadata = pageMetadata("/contato", "Contato"); export default async function Page() { const [{ locale }, profile] = await Promise.all([getPreferences(), getCachedProfile()]); return <main id="content"><Contact locale={locale}/><Footer locale={locale} profile={profile}/></main>; }
