import { Resume, Footer } from "@/components/server-content"; import { getPreferences } from "@/lib/preferences"; import { pageMetadata } from "@/lib/seo"; export const metadata = pageMetadata("/curriculo", "Currículo"); export default async function Page() { const { locale } = await getPreferences(); return <main id="content"><Resume locale={locale}/><Footer locale={locale}/></main>; }
