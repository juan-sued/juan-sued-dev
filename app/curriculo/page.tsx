import { Resume, Footer } from "@/components/server-content"; import { getPreferences } from "@/lib/preferences"; import { getCachedProfile } from "@/lib/repositories/profile-cache"; import { pageMetadata } from "@/lib/seo"; export const metadata = pageMetadata("/curriculo", "Currículo"); export default async function Page() { const [{ locale }, profile] = await Promise.all([getPreferences(), getCachedProfile()]); return <main id="content"><Resume locale={locale}/><Footer locale={locale} profile={profile}/></main>; }
