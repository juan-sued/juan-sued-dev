import { Resume, Footer } from "@/components/server-content"; import { getPreferences } from "@/lib/preferences"; import { getCachedProfile } from "@/lib/repositories/profile-cache"; import { getCachedResumeUrls } from "@/lib/repositories/resumes-cache"; import { pageMetadata } from "@/lib/seo"; export const metadata = pageMetadata("/curriculo", "Currículo"); export default async function Page() { const [{ locale }, profile, resumeUrls] = await Promise.all([getPreferences(), getCachedProfile(), getCachedResumeUrls()]); return <main id="content"><Resume locale={locale} resumeUrls={resumeUrls}/><Footer locale={locale} profile={profile}/></main>; }
