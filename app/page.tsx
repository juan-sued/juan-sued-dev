import { Home, Footer } from "@/components/server-content"; import { getPreferences } from "@/lib/preferences"; import { getEducation } from "@/lib/repositories/education"; import { getExperiences } from "@/lib/repositories/experiences"; import { getSkills } from "@/lib/repositories/skills"; import { pageMetadata } from "@/lib/seo"; export const metadata = pageMetadata("/", "Juan Sued - Desenvolvedor Full Stack Web & Mobile"); export default async function Page() { const [{ locale }, experiences, skills, education] = await Promise.all([getPreferences(), getExperiences(), getSkills(), getEducation()]); return <main id="content"><Home locale={locale} experiences={experiences} skills={skills} education={education}/><Footer locale={locale}/></main>; }
