import { Home, Footer } from "@/components/server-content"; import { getPreferences } from "@/lib/preferences"; import { pageMetadata } from "@/lib/seo"; export const metadata = pageMetadata("/", "Juan Sued - Desenvolvedor Full Stack Web & Mobile"); export default async function Page() { const { locale } = await getPreferences(); return <main id="content"><Home locale={locale}/><Footer locale={locale}/></main>; }
