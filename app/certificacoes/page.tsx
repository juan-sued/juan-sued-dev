import { CertificationsPage, Footer } from "@/components/server-content"; import { getPreferences } from "@/lib/preferences"; import { loadCertifications } from "@/lib/repositories/certifications-cache"; import { pageMetadata } from "@/lib/seo"; export const metadata = pageMetadata("/certificacoes", "Certificações"); export default async function Page() { const { locale, recruiter } = await getPreferences(); const certifications = await loadCertifications(locale, recruiter); return <main id="content"><CertificationsPage locale={locale} certifications={certifications}/><Footer locale={locale}/></main>; }
