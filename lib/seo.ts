import type { Metadata } from "next";
import { siteName, siteUrl } from "@/lib/site";
const defaultDescription = "Desenvolvedor Full Stack especializado em React, React Native, TypeScript, Node.js e NestJS, com experiência em geolocalização, sistemas em tempo real e aplicações Web e Mobile.";
export function pageMetadata(path: string, title: string, description = defaultDescription): Metadata { const url = `${siteUrl}${path}`; return { title, description, alternates: { canonical: url }, openGraph: { title: `${title} | ${siteName}`, description, url, siteName, locale: "pt_BR", type: "website", images: [{ url: "/og-image.png", width: 1200, height: 630, alt: "Juan Sued - Desenvolvedor Full Stack Web & Mobile" }] }, twitter: { card: "summary_large_image", title: `${title} | ${siteName}`, description, images: ["/og-image.png"] } }; }
